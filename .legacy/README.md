# Legacy Scripts

This folder keeps deprecated scripts that are no longer part of the active setup.

Guidelines:
- Do not call scripts here from `install.sh`, `setup/`, `config/`, or `bin/`.
- Keep these files only for reference or rollback.
- Prefer moving unused scripts here instead of deleting immediately.
