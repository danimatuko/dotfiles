#!/bin/bash

# Fail fast and keep wrapper behavior predictable.
set -euo pipefail

# Wrapper entrypoint in PATH; delegates to the real script in dotfiles.
exec "$HOME/dotfiles/scripts/toggle-nightlight.sh" "$@"
