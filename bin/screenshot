#!/bin/bash

# Load XDG user directories (e.g., XDG_PICTURES_DIR)
[[ -f ~/.config/user-dirs.dirs ]] && source ~/.config/user-dirs.dirs

# Use Screenshots inside system Pictures folder, or fallback
PICTURES_DIR="${XDG_PICTURES_DIR:-$HOME/Pictures}"
OUTPUT_DIR="$PICTURES_DIR/Screenshots"
OUTPUT_FILE="$OUTPUT_DIR/screenshot-$(date +'%d-%m-%Y_%H-%M-%S').png"

mkdir -p "$OUTPUT_DIR"

MODE="${1:-region}"

if [[ "$MODE" == "output" || "$MODE" == "screen" ]]; then
  HYPRSHOT_ARGS=(-m output -m active)
else
  HYPRSHOT_ARGS=(-m "$MODE")
fi

pkill slurp >/dev/null 2>&1 || true

hyprshot "${HYPRSHOT_ARGS[@]}" --raw |
  satty --filename - \
        --output-filename "$OUTPUT_FILE" \
        --early-exit \
        --actions-on-enter save-to-clipboard \
        --save-after-copy \
        --copy-command 'wl-copy'
